import styled from '@emotion/styled';

export const Container = styled.div`
    width: 700px;
    margin-inline: auto;
    display: flex;
    flex-direction: column;
    gap: 50px;
    color: #110101;
`;